(* type _ g = MkG : 'a. 'a g

   let g () = f (-#1l) #10l

   let g () = f #1l #10l

   let g () = f 1l 10l

   let f x = match x with -1l | -2l -> true | _ -> false

   let f x = match x with 1l | 2l -> true | _ -> false

   let f x = match x with -#1l | -#2l -> true | _ -> false *)
